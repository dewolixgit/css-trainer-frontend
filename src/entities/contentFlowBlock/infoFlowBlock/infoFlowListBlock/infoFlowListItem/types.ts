export interface IInfoFlowListItem {
  id: number;
  text: string;
}

export type InfoFlowListItemParams = {
  id: number;
  text: string;
};

export type InfoFlowListItemApi = {
  id: number;
  text: string;
};

export enum ThematicLoaderTypeEnum {
  sword = 'sword',
}

export enum ThematicLoaderAnimationTypeEnum {
  // Spinner
  spin = 'spin',

  // Pendulum, pivot point in the center
  pendulum = 'pendulum',

  // Pendulum, pivot point in the bottom center
  pendulumBottomCenter = 'pendulum-bottom-center',
}

export enum ThematicLoaderColor {
  primary = 'primary',
  secondary = 'secondary',
}
